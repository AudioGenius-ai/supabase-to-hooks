/**
 * Basic test suite to verify Jest setup
 */

describe('Jest Setup', () => {
  it('should work properly', () => {
    expect(true).toBe(true);
  });

  it('should handle basic math', () => {
    expect(1 + 1).toBe(2);
  });
}); 